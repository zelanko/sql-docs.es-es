---
title: "Establecer las propiedades de conexión | Documentos de Microsoft"
ms.custom: 
ms.date: 04/11/2017
ms.prod: sql-non-specified
ms.reviewer: 
ms.suite: 
ms.technology:
- drivers
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: f1b62700-f046-488d-bd6b-a5cd8fc345b7
caps.latest.revision: 154
author: MightyPen
ms.author: genemi
manager: jhubbard
ms.workload: Active
ms.translationtype: MT
ms.sourcegitcommit: f7e6274d77a9cdd4de6cbcaef559ca99f77b3608
ms.openlocfilehash: 56dbb81f9d30ccb41a6247c4fefda9d6ecd703a6
ms.contentlocale: es-es
ms.lasthandoff: 09/09/2017

---
# <a name="setting-the-connection-properties"></a>Establecer las propiedades de conexión
[!INCLUDE[Driver_JDBC_Download](../../includes/driver_jdbc_download.md)]

  Las propiedades de las cadenas de conexión se pueden especificar de diversas formas:  
  
-   Como nombre = propiedades de valor en la dirección URL de conexión cuando se conecta mediante el uso de la clase DriverManager.  
  
-   Como nombre = valor propiedades en el *propiedades* parámetro del método Connect en la clase DriverManager.  
  
-   Como valores en el método establecedor adecuado del origen de datos del controlador. Por ejemplo:  
  
    ```  
  
    datasource.setServerName(value)  
    datasource.setDatabaseName(value)  
  
    ```  
  
## <a name="remarks"></a>Comentarios  
 En los nombres de las propiedades no se distinguen entre mayúsculas y minúsculas, y los duplicados se resuelven de la siguiente forma:  
  
1.  Argumentos de API (como usuario y contraseña)  
  
2.  Colección de propiedades.  
  
3.  Última instancia de la cadena de conexión.  
  
 Además, se permiten valores desconocidos para los nombres de propiedades, el controlador JDBC no valida sus valores en relación a la distinción entre mayúsculas y minúsculas.  
  
 Se permite el uso de sinónimos y se resuelven por orden, igual que los nombres de propiedad duplicados.  
  
 La siguiente tabla muestra todas las propiedades de cadena de conexión disponibles actualmente para el controlador JDBC.  
  
|Propiedad|Tipo|Predeterminado|Description|  
|--------------|----------|-------------|-----------------|  
|accessToken|String|null|Utilice esta propiedad para conectarse a una base de datos SQL con un token de acceso. **accessToken** no se puede establecer utilizando la dirección URL de conexión.|  
|applicationIntent|String|ReadWrite|Declara el tipo de carga de trabajo de la aplicación al conectarse a un servidor. Los valores posibles son **ReadOnly** y **ReadWrite**. Para obtener más información, consulte [compatibilidad del controlador JDBC con alta disponibilidad y recuperación ante desastres](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md).|  
|applicationName|String<br /><br /> [<=128 char]|null|El nombre de la aplicación, o "[!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)]" Si no se proporciona ningún nombre. Usado para identificar la aplicación específica en diversas [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] de generación de perfiles y herramientas de registro.|  
|autenticación|String|NotSpecified|Comenzar con Microsoft JDBC Driver 6.0 para SQL Server, esta propiedad opcional indica qué método de autenticación de SQL que se utilizará para la conexión. Los valores posibles son **ActiveDirectoryIntegrated**, **ActiveDirectoryPassword**, **SqlPassword** y el valor predeterminado **NotSpecified**.<br /><br /> Use **ActiveDirectoryIntegrated** para conectarse a una base de datos de SQL mediante la autenticación integrada de Windows.<br /><br /> Use **ActiveDirectoryPassword** para conectarse a una base de datos de SQL mediante un nombre de entidad de seguridad de Azure AD y una contraseña.<br /><br /> Use **SqlPassword** para conectarse a un servidor SQL utilizando **nombre de usuario**/**usuario** y **contraseña** propiedades.<br /><br /> Use **NotSpecified** si se necesita ninguno de estos métodos de autenticación.<br /><br /> **Importante:** si la autenticación se establece en ActiveDirectoryIntegrated, las dos bibliotecas siguientes deberá instalarse: **SQLJDBC_AUTH. DLL** (disponible en el paquete de controladores JDBC) y Azure biblioteca de autenticación de Active Directory para SQL Server (**ADALSQL. DLL**) está disponible en varios idiomas (x86 y amd64) desde el centro de descarga en [Microsoft biblioteca de autenticación de Active Directory para Microsoft SQL Server](https://www.microsoft.com/en-us/download/details.aspx?id=48742). El controlador JDBC sólo es compatible con la versión **1.0.2028.318 y versiones posteriores** para el ADALSQL. DLL.<br /><br /> **Nota:** cuando la propiedad de autenticación se establece en cualquier valor distinto de **NotSpecified**, el controlador de forma predeterminada utiliza el cifrado de capa de Sockets seguros (SSL).<br /><br /> Para obtener información sobre cómo configurar la autenticación de Active Directory de Azure, visite [conectarse a SQL base de datos mediante Azure Active Directory autenticación](https://azure.microsoft.com/en-us/documentation/articles/sql-database-aad-authentication/).|  
|authenticationScheme|String|"NativeAuthentication"|Indica qué tipo de seguridad integrada desea que use la aplicación. Los valores posibles son **Kerberos** y el valor predeterminado **NativeAuthentication**.<br /><br /> Cuando se usa **authenticationScheme = Kerberos**, debe especificar el nombre de dominio completo (FQDN) en el **serverName** o **serverSpn** propiedad. De lo contrario, se producirá un error (No se encontró el servidor en la base de datos de Kerberos).<br /><br /> Para obtener más información sobre el uso de **authenticationScheme**, consulte [utilizando la autenticación integrada Kerberos para conectarse a SQL Server](../../connect/jdbc/using-kerberos-integrated-authentication-to-connect-to-sql-server.md).|  
|columnEncryptionSetting|String<br /><br /> ["Habilitado" &#124;" Deshabilitado"]|Deshabilitado|Establecer en "Enabled" para utilizar el principio de la característica Always Encrypted (AE) con Microsoft JDBC Driver 6.0 para SQL Server. Cuando se habilita AE, el controlador JDBC cifra y descifra de forma transparente los datos confidenciales almacenados en columnas de base de datos cifradas de SQL Server.<br /><br /> Vea [usar Always Encrypted con el controlador JDBC](../../connect/jdbc/using-always-encrypted-with-the-jdbc-driver.md) para obtener más detalles.<br /><br /> **Nota:** Always Encrypted está disponible con SQL Server 2016 o versiones posteriores.|  
|databaseName, base de datos|String<br /><br /> [<=128 char]|null|Nombre de la base de datos a la que se conectará. Si no se especifica, se establece una conexión a la base de datos predeterminada.|  
|disableStatementPooling|boolean<br /><br /> ["true" &#124;" False"]|true|Actualmente solo se admite el valor "true". Si se estableciera en "false", se produciría una excepción.|  
|encrypt|boolean<br /><br /> ["true" &#124;" False"]|false|Establézcala en "true" para especificar que la [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] utiliza el cifrado de capa de Sockets seguros (SSL) para todos los datos enviados entre el cliente y el servidor si el servidor tiene instalado un certificado. El valor predeterminado es false.<br /><br /> Empezando con Microsoft JDBC Driver 6.0 para SQL Server, hay una nueva conexión setting 'authentication' que utiliza el cifrado SSL de forma predeterminada. Para obtener más información, vea la propiedad 'authentication'.|  
|failoverPartner|String|null|Nombre del servidor de conmutación por error que se usa en la configuración de la creación de reflejo de la base de datos. Esta propiedad se usa para un error de conexión inicial con el servidor principal; una vez establecida la conexión inicial, se omite esta propiedad. Se debe usar junto con la propiedad databaseName.<br /><br /> **Nota:** el controlador no admite especificar el número de puerto de la instancia de servidor para la instancia del asociado de conmutación por error como parte de la propiedad failoverPartner en la cadena de conexión. Sin embargo, admite especificar las propiedades serverName, instanceName y portNumber de la instancia del servidor principal failoverPartner y la instancia del asociado de conmutación por error en la misma cadena de conexión.<br /><br /> Si especifica un nombre de red Virtual en el **Server** propiedad de conexión, no puede usar la creación de reflejo de base de datos. Vea [compatibilidad del controlador JDBC con alta disponibilidad y recuperación ante desastres](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md) para obtener más información.|  
|hostNameInCertificate|String|null|El nombre de host que se usará para validar la [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] certificado SSL.<br /><br /> Si la propiedad hostNameInCertificate está especificado o está establecida en null, el [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] usará la **serverName** valor de propiedad en la dirección URL de conexión como el nombre de host para validar la [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] certificado SSL.<br /><br /> **Nota:** esta propiedad se utiliza en combinación con la **cifrar**/**autenticación** propiedades y **trustServerCertificate**propiedad. Esta propiedad afecta a la validación del certificado, si y solo si la conexión utiliza el cifrado de capa de Sockets seguros (SSL) y la **trustServerCertificate** se establece en "false". Asegúrese de que el valor pasado a **hostNameInCertificate** coincide exactamente con el nombre DNS o nombre común (CN) en el nombre alternativo sujeto (SAN) en el certificado de servidor para una conexión SSL sea correcta. Para obtener más información, consulte [descripción de la compatibilidad SSL](../../connect/jdbc/understanding-ssl-support.md).|  
|NOMBREINSTANCIA|String<br /><br /> [<=128 char]|null|El [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] nombre de instancia para conectarse a. Si no se especifica, se establece una conexión a la instancia predeterminada. En el caso de que se especifique el puerto e instanceName, consulte las notas referentes al puerto.<br /><br /> Si especifica un nombre de red Virtual en el **Server** propiedad de conexión, no puede usar **nombreDeInstancia** propiedad de conexión. Vea [compatibilidad del controlador JDBC con alta disponibilidad y recuperación ante desastres](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md) para obtener más información.|  
|integratedSecurity|boolean<br /><br /> ["true" &#124;" False"]|false|Establézcalo en "true" para indicar que va a usar credenciales de Windows por [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] en sistemas operativos Windows. Si el valor es "true", el controlador JDBC busca en la memoria caché de credenciales del equipo local suministradas en el equipo o en el inicio de sesión de red.<br /><br /> Establecido en "true" (con **authenticationscheme = Kerberos**), para indicar que va a usar credenciales Kerberos por [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)]. Para obtener más información sobre la autenticación Kerberos, vea [utilizando la autenticación integrada Kerberos para conectarse a SQL Server](../../connect/jdbc/using-kerberos-integrated-authentication-to-connect-to-sql-server.md). <br /><br /> Si el valor es "false", se deben suministrar el nombre de usuario y la contraseña.| 
|keyStoreAuthentication|String|null| Comenzar con Microsoft JDBC Driver 6.0 para SQL Server, esta propiedad identifica qué almacén de claves sin problemas configurados para la conexión con Always Encrypted y determina un mecanismo de autenticación que se utiliza para autenticarse en el almacén de claves. Microsoft JDBC Driver 6.0 para SQL Server admite configuración hasta del almacén de claves de Java de forma transparente mediante esta propiedad para el que debe establecer "**keyStoreAuthentication = JavaKeyStorePassword**". Tenga en cuenta que para utilizar esta propiedad, también debe establecer el **keyStoreLocation** y **keyStoreSecret** propiedades para el almacén de claves de Java. <br/><br/>Para obtener más información, visite [usar Always Encrypted con el controlador JDBC](https://msdn.microsoft.com/library/mt591987%28v=sql.110%29.aspx?f=255&MSPPError=-2147217396). 
|keyStoreLocation|String|null|Cuando **keyStoreAuthentication = JavaKeyStorePassword**, **keyStoreLocation** propiedad identifica la ruta de acceso del archivo de almacén de claves de Java que almacena la clave maestra de columna que se usarán con Always Encrypted datos. Tenga en cuenta que la ruta de acceso debe incluir el nombre de archivo de almacén de claves.<br/><br/>Para obtener más información, visite [usar Always Encrypted con el controlador JDBC](https://msdn.microsoft.com/library/mt591987%28v=sql.110%29.aspx?f=255&MSPPError=-2147217396).
|keyStoreSecret|String|null|Cuando **keyStoreAuthentication = JavaKeyStorePassword**, **keyStoreSecret** propiedad identifica la contraseña que se utilizará para el almacén de claves, así como para la clave. Tenga en cuenta que para utilizar el almacén de claves de Java el almacén de claves y la contraseña de la clave deben ser la misma.<br/><br/>Para obtener más información, visite [usar Always Encrypted con el controlador JDBC](https://msdn.microsoft.com/library/mt591987%28v=sql.110%29.aspx?f=255&MSPPError=-2147217396).
|lastUpdateCount|boolean<br /><br /> ["true" &#124;" False"]|true|El valor "true" solamente devuelve el último recuento de actualizaciones de una instrucción SQL pasada al servidor y se puede usar en instrucciones SELECT, INSERT o DELETE individuales para omitir los recuentos de actualizaciones adicionales generados por los desencadenadores del servidor. Si se establece esta propiedad en "false", se devuelven todos los recuentos de actualizaciones, incluidos los devueltos por los desencadenadores del servidor.<br /><br /> **Nota:** esta propiedad solo se aplica cuando se usa con la [executeUpdate](../../connect/jdbc/reference/executeupdate-method-sqlserverstatement.md) métodos. Todos los demás ejecutar métodos devuelven todos los resultados y recuentos de actualizaciones. Esta propiedad solamente afecta a los recuentos de actualizaciones devueltos por los desencadenadores del servidor. No afecta a los conjuntos de resultados o errores que resultan como parte de la ejecución del desencadenador.|  
|lockTimeout|int|-1|El número de milisegundos que hay que esperar antes de que la base de datos informe del tiempo de espera para la exclusión. El comportamiento predeterminado es esperar indefinidamente. Si se especifica, este valor será el predeterminado para todas las instrucciones de la conexión. Tenga en cuenta que **Statement.setQueryTimeout()** puede usarse para establecer el tiempo de espera para instrucciones específicas. El valor puede ser 0, lo que significa que no hay espera.|  
|loginTimeout|int [0..65535]|15|Número de segundos que debería esperar el controlador antes de agotar el tiempo de espera de una conexión errónea. Un valor cero indica que el tiempo de espera es el predeterminado del sistema, que está especificado en 15 segundos de manera predeterminada. Un valor diferente a cero es el número de segundos que debería esperar el controlador antes de agotar el tiempo de espera de una conexión errónea.<br /><br /> Si especifica un nombre de red Virtual en el **Server** propiedad de conexión, debe especificar un valor de tiempo de espera de tres minutos como mínimo para dejar tiempo suficiente para una conexión de conmutación por error sea correcta. Vea [compatibilidad del controlador JDBC con alta disponibilidad y recuperación ante desastres](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md) para obtener más información.|  
|multiSubnetFailover|Boolean|false|Especifique siempre **multiSubnetFailover = true** al conectarse a la escucha del grupo de disponibilidad de un [!INCLUDE[ssSQL11](../../includes/sssql11_md.md)] grupo de disponibilidad o una [!INCLUDE[ssSQL11](../../includes/sssql11_md.md)] instancia de clúster de conmutación por error. **multiSubnetFailover = true** configura [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] para proporcionar una detección más rápida y conexión con el servidor (actualmente) activo. Los valores posibles son true y false. Vea [compatibilidad del controlador JDBC con alta disponibilidad y recuperación ante desastres](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md) para obtener más información.<br /><br /> Mediante programación puede tener acceso a la **multiSubnetFailover** propiedad de conexión con [getPropertyInfo](../../connect/jdbc/reference/getpropertyinfo-method-sqlserverdriver.md), [getMultiSubnetFailover](../../connect/jdbc/reference/getmultisubnetfailover-method-sqlserverdatasource.md), y [ setMultiSubnetFailover](../../connect/jdbc/reference/setmultisubnetfailover-method-sqlserverdatasource.md).<br /><br /> **Nota:** a partir de Microsoft JDBC Driver 6.0 para SQL Server ya no es necesario establecer multiSubnetFailover en true cuando se conecta a un agente de escucha del grupo de disponibilidad. Una propiedad nueva, **transparentNetworkIPResolution**, que está habilitada de forma predeterminada, proporciona la detección y la conexión al servidor (actualmente) activo.|  
|packetSize|int [-1 &#124; 0 &#124; 512..32767]|8000|El tamaño del paquete de red se usa para establecer la comunicación con SQL Server y se especifica en bytes. El valor -1 indica que se usa el tamaño de paquete de servidor predeterminado. El valor 0 indica que se usa el valor máximo, que es 32767. Si esta propiedad se establece en un valor fuera del rango aceptable, se produce una excepción.<br /><br /> **Importante:** no se recomienda utilizar la propiedad packetSize cuando está habilitado el cifrado (cifrar = true). De lo contrario, el controlador podría generar un error de conexión. Para obtener más información, consulte el [setPacketSize](../../connect/jdbc/reference/setpacketsize-method-sqlserverdatasource.md) método de la [SQLServerDataSource](../../connect/jdbc/reference/sqlserverdatasource-class.md) clase.|  
|password|String<br /><br /> [<=128 char]|null|La contraseña de la base de datos, en el caso de conexión con el usuario SQL y una contraseña.<BR/>Para conexiones de Kerberos con el nombre de entidad de seguridad y una contraseña, esta propiedad se establece en la contraseña de la entidad de seguridad de Kerberos.|  
|portNumber, puerto|int [0..65535]|1433|El puerto donde [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] está escuchando. Si se especifica el número del puerto en la cadena de conexión, no se realiza ninguna solicitud a sqlbrowser. Si se especifican el puerto e instanceName, se establece la conexión con el puerto especificado. Sin embargo, el **nombreDeInstancia** se valida y se produce un error si no coincide con el puerto.<br /><br /> **Importante:** se recomienda que siempre se especifique el número de puerto, puesto que éste es más seguro que usar sqlbrowser.|  
|responseBuffering|String<br /><br /> ["completa" &#124;" ADAPTATIVE"]|adaptive|Si esta propiedad se establece en "adaptive", los datos mínimos posibles se almacenan en búfer cuando es necesario. El modo predeterminado es "adaptative".<br /><br /> Cuando esta propiedad se establece en "full", el conjunto de resultados completo se lee del servidor cuando se ejecuta una instrucción.<br /><br /> **Nota:** después de actualizar el controlador JDBC desde la versión 1.2, el almacenamiento en búfer de comportamiento de predeterminado será "adaptive". Si la aplicación nunca ha establecido la propiedad "responseBuffering" y desea mantener el comportamiento predeterminado de la versión 1.2 en su aplicación, debe establecer la propiedad responseBufferring en "full" en las propiedades de conexión o mediante el [ setResponseBuffering](../../connect/jdbc/reference/setresponsebuffering-method-sqlserverstatement.md) método de la [SQLServerStatement](../../connect/jdbc/reference/sqlserverstatement-class.md) objeto.|  
|selectMethod|String<br /><br /> ["directo" &#124;" cursor"]|directo|Si esta propiedad se establece en "cursor", se crea un cursor de base de datos para cada consulta que se cree en la conexión para los cursores TYPE_FORWARD_ONLY y CONCUR_READ_ONLY. Esta propiedad normalmente solo es necesaria si la aplicación genera conjuntos de resultados muy grandes que no se pueden contener completamente en la memoria del cliente. Cuando se establece esta propiedad en "cursor", solo se retienen en la memoria del cliente un número limitado de filas de los conjuntos de resultados. El comportamiento predeterminado es retener en la memoria del cliente todas las filas de los conjuntos de resultados. Este comportamiento proporciona el rendimiento más rápido cuando la aplicación va a procesar todas las filas.|  
|sendStringParametersAsUnicode|boolean<br /><br /> ["true" &#124;" False"]|true|Si el **sendStringParametersAsUnicode** propiedad se establece en "true", la cadena de parámetros se envían al servidor en formato Unicode.<br /><br /> Si el **sendStringParametersAsUnicode** propiedad está establecida en "false", se envían parámetros String al servidor en formato que no son Unicode, como ASCII/MBCS Unicode.<br /><br /> El valor predeterminado para la **sendStringParametersAsUnicode** propiedad es "true".<br /><br /> **Nota:** el **sendStringParametersAsUnicode** propiedad solo se comprueba cuando se envía un valor de parámetro con **CHAR**, **VARCHAR**, o **LONGVARCHAR** tipos de JDBC. Los nuevos métodos de caracteres nacionales de JDBC 4.0, como los métodos setNString, setNCharacterStream y setNClob de [SQLServerPreparedStatement](../../connect/jdbc/reference/sqlserverpreparedstatement-class.md) y [SQLServerCallableStatement](../../connect/jdbc/reference/sqlservercallablestatement-class.md) clases, siempre enviar sus valores de parámetro al servidor en formato Unicode, independientemente del valor de esta propiedad.<br /><br /> Para obtener un rendimiento óptimo con el **CHAR**, **VARCHAR**, y **LONGVARCHAR** tipos de datos JDBC, una aplicación debe establecer el ** sendStringParametersAsUnicode** propiedad en "false" y use la setString, setCharacterStream y métodos de caracteres no nacionales setClob de la [SQLServerPreparedStatement](../../connect/jdbc/reference/sqlserverpreparedstatement-class.md) y [ SQLServerCallableStatement](../../connect/jdbc/reference/sqlservercallablestatement-class.md) clases.<br /><br /> Cuando la aplicación establece el **sendStringParametersAsUnicode** propiedad en "false" y usa tipos de un método de caracteres no nacionales para obtener acceso a los datos Unicode en el servidor (como **nchar**, **nvarchar** y **ntext**), se pueden perder algunos datos si la intercalación de base de datos no admite los caracteres de los parámetros String pasados por el método de caracteres nacionales.<br /><br /> Tenga en cuenta que una aplicación debe utilizar los métodos de caracteres nacionales setNString, setNCharacterStream y setNClob de la [SQLServerPreparedStatement](../../connect/jdbc/reference/sqlserverpreparedstatement-class.md) y [SQLServerCallableStatement](../../connect/jdbc/reference/sqlservercallablestatement-class.md) clases para el **NCHAR**, **NVARCHAR**, y **LONGNVARCHAR** tipos de datos JDBC.|  
|sendTimeAsDatetime|boolean<br /><br /> ["true" &#124;" False"]|true|Esta propiedad se agregó en [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] controlador JDBC 3.0.<br /><br /> Cuando sea true, los valores java.sql.Time se enviarán al servidor como [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] **datetime** valores.<br /><br /> Cuando sea false, los valores java.sql.Time se enviarán al servidor como [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] **tiempo** valores.<br /><br /> **sendTimeAsDatetime** también se pueden modificar mediante programación con [SQLServerDataSource.setSendTimeAsDatetime](../../connect/jdbc/reference/setsendtimeasdatetime-method-sqlserverdatasource.md).<br /><br /> El valor predeterminado de esta propiedad podría verse modificado en una versión futura.<br /><br /> Para obtener más información acerca de cómo los [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] configura los valores java.sql.Time antes de enviarlos al servidor, consulte [cómo configurar los valores java.sql.Time se envían al servidor](../../connect/jdbc/configuring-how-java-sql-time-values-are-sent-to-the-server.md).|  
|serverName, servidor|String|null|El equipo que ejecuta [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)].<br /><br /> También puede especificar el nombre de red virtual de un grupo de disponibilidad [!INCLUDE[ssHADR](../../includes/sshadr_md.md)]. Vea [compatibilidad del controlador JDBC con alta disponibilidad y recuperación ante desastres](../../connect/jdbc/jdbc-driver-support-for-high-availability-disaster-recovery.md) para obtener más información.|  
|serverSpn|String|null|A partir de Microsoft JDBC Driver 4.2 para SQL Server, esta propiedad opcional puede usarse para especificar el nombre principal de servicio (SPN) para una conexión Kerberos de Java.  Se utiliza junto con **authenticationScheme**.<br /><br /> Para especificar el SPN, puede ser en forma de: "MSSQLSvc/fqdn:port@REALM" donde fqdn es el nombre de dominio completo, port es el número de puerto y el territorio es el dominio Kerberos de SQL Server en letras mayúsculas.<br /><br /> Nota: la @REALM es opcional si el dominio Kerberos predeterminado del cliente (como se especifica en la configuración de Kerberos) es el mismo que el dominio de Kerberos para SQL Server.<br /><br /> Para obtener más información sobre el uso de **serverSpn** con Kerberos de Java, consulte [utilizando la autenticación integrada Kerberos para conectarse a SQL Server](../../connect/jdbc/using-kerberos-integrated-authentication-to-connect-to-sql-server.md).|  
|serverNameAsACE|boolean<br /><br /> ["true" &#124;" False"]|false|A partir de Microsoft JDBC Driver 6.0 para SQL Server, se establece en "true" para indicar que el controlador debe traducir el nombre de servidor Unicode a una codificación compatible con ASCII (Punycode) para la conexión. Si este valor se establece en false, el controlador se conecta mediante el nombre de servidor tal como lo proporciona el usuario.<br /><br /> Vea [características internacionales del controlador JDBC](../../connect/jdbc/international-features-of-the-jdbc-driver.md) para obtener más detalles.|  
|TransparentNetworkIPResolution|boolean<br /><br /> ["true" &#124;" False"]|true|Desde Microsoft JDBC Driver 6.0 para SQL Server, esta propiedad, transparentNetworkIPResolution, proporciona una detección más rápida y conexión con el servidor (actualmente) activo. Valores posibles son true y false a true como valor predeterminado.<br /><br /> Antes de Microsoft JDBC Driver 6.0 para SQL Server, una aplicación tenía que establecer la cadena de conexión debe incluir "multiSubnetFailover = true" para indicar que se conecta a un grupo de disponibilidad AlwaysOn. Sin establecer la palabra clave de conexión de multiSubnetFailover en true, una aplicación podría experimentar un tiempo de espera mientras se conecta a un grupo de disponibilidad AlwaysOn. Comenzar con Microsoft JDBC Driver 6.0 para SQL Server, una aplicación no necesite establecer multiSubnetFailover en true ya. <br /><br />**Nota:** cuando transparentNetworkIPResolution = true, el primer intento de conexión que utiliza la 500 ms como el tiempo de espera. Los intentos posteriores utilice la misma lógica de tiempo de espera utilizados por la propiedad multiSubnetFailover.|  
|columnEncryptionSetting|boolean<br /><br /> ["true" &#124;" False"]|false|Se establece en "true" para usar la característica Always Encrypted (AE) con Microsoft JDBC Driver 6.0 para SQL Server. Cuando se habilita AE, el controlador JDBC cifra y descifra de forma transparente los datos confidenciales almacenados en columnas de base de datos cifradas de SQL Server.<br /><br /> Vea [usar Always Encrypted con el controlador JDBC](../../connect/jdbc/using-always-encrypted-with-the-jdbc-driver.md) para obtener más detalles.<br /><br /> **Nota:** Always Encrypted está disponible con SQL Server 2016 o versiones más recientes.|  
|trustServerCertificate|boolean<br /><br /> ["true" &#124;" False"]|false|Establézcala en "true" para especificar que la [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] no validará el [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] certificado SSL.<br /><br /> Si es "true", la [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] certificado SSL se confía automáticamente cuando el nivel de comunicación se cifra mediante SSL.<br /><br /> Si es "false", la [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] validará el certificado SSL de servidor. Si la validación del certificado de servidor da error, el controlador generará un error y terminará la conexión. El valor predeterminado es "false". Asegúrese de que el valor pasado a **serverName** coincide exactamente con el nombre DNS o nombre común (CN) en el nombre alternativo del sujeto del certificado de servidor para una conexión SSL sea correcta. Para obtener más información, consulte [descripción de la compatibilidad SSL](../../connect/jdbc/understanding-ssl-support.md).<br /><br /> **Nota:** esta propiedad se utiliza en combinación con la **cifrar**/**autenticación** propiedades. Esta propiedad solo afecta a la validación del certificado SSL de servidor si y solo si la conexión utiliza el cifrado SSL.|  
|trustStore|String|null|La ruta de acceso (incluido el nombre de archivo) del archivo trustStore del certificado. El archivo trustStore contiene la lista de certificados en los que el cliente confía.<br /><br /> Cuando esta propiedad no se especifica o se establece en null, el controlador se basará en las reglas de búsqueda del generador del administrador de confianza para determinar qué almacén de certificados se usará.<br /><br /> **El generador TrustManagerFactory SunX509 predeterminado intenta buscar material de confianza en el siguiente orden de búsqueda:**<br /><br /> Un archivo especificado por la propiedad del sistema "javax.net.ssl.trustStore" de la máquina virtual Java (JVM).<br /><br /> "\<java-home >/lib/security/jssecacerts" archivo.<br /><br /> "\<java-home >/lib/security/cacerts" archivo.<br /><br /> <br /><br /> Para obtener más información, consulte la documentación de la interfaz de SUNX509 TrustManager en el sitio Web de Sun Microsystems.<br /><br /> **Nota:** esta propiedad solo afecta a la búsqueda de trustStore de certificado, si y solo si la conexión utiliza el cifrado SSL y la **trustServerCertificate** propiedad se establece en "false".|  
|trustStorePassword|String|null|Contraseña que se usa para comprobar la integridad de los datos trustStore.<br /><br /> Si se establece la propiedad trustStore pero no se establece la propiedad trustStorePassword, la integridad de trustStore no se comprueba.<br /><br /> Cuando las propiedades trustStore y trustStorePassword no se especifican, el controlador utilizará las propiedades del sistema de JVM, "javax.net.ssl.trustStore" y "javax.net.ssl.trustStorePassword". Si no se especifica la propiedad del sistema "javax.net.ssl.trustStorePassword", no se comprueba la integridad del trustStore.<br /><br /> Si no se establece la propiedad trustStore pero se establece la propiedad trustStorePassword, el controlador JDBC utilizará el archivo especificado por "javax.net.ssl.trustStore" como almacén de confianza y la integridad de éste se comprueba con la trustStorePassword especificada. Esto podría ser necesario cuando la aplicación cliente no desea almacenar la contraseña en la propiedad del sistema de JVM.<br /><br /> **Nota:** la propiedad trustStorePassword solo afecta a la búsqueda de trustStore de certificado, si y solo si la conexión utiliza la conexión SSL y la **trustServerCertificate** propiedad se establece en "false".|  
|userName, usuario|String<br /><br /> [<=128 char]|null|El usuario de base de datos, en el caso de conexión con el usuario SQL y una contraseña.<BR/>Para la conexión de Kerberos con el nombre de entidad de seguridad y la contraseña, esta propiedad se establece como nombre Principal de Kerberos.|  
|workstationID|String<br /><br /> [<=128 char]|\<una cadena vacía >|Identificador de la estación de trabajo. Usado para identificar la estación de trabajo concreta en diversas [!INCLUDE[ssNoVersion](../../includes/ssnoversion_md.md)] de generación de perfiles y herramientas de registro. Si no se especifica ninguno, el \<una cadena vacía > se utiliza.|  
|xopenStates|boolean<br /><br /> ["true" &#124;" False"]|false|Establézcalo en "true" para especificar que el controlador devuelve en las excepciones códigos de estado compatibles con XOPEN. De forma predeterminada se devuelven códigos de estado SQL 99.|  
|gsscredential|org.ietf.jgss.GSSCredential|null|Desde 6.2 de controlador JDBC de Microsoft para SQL Server, se pueden pasar las credenciales de usuario que se usará para la delegación limitada de Kerberos en esta propiedad. <BR/>Esto se debe utilizar con **integratedSecurity** como **true** y **Kerberos** **authenticationscheme**.| 
|jaasConfigurationName|String|SQLJDBCDriver|Comenzar con Microsoft JDBC Driver 6.2 para SQL Server, cada conexión a SQL Server puede tener su propio archivo de configuración de inicio de sesión JAAS para establecer conexión de Kerberos. Nombre del archivo de configuración de inicio de sesión se puede pasar a través de esta propiedad. <BR/> De forma predeterminada, el controlador establece propiedad `useDefaultCcache = true` para JVMs de IBM, y `useTicketCache = true` para otros JVMs.|
|serverPreparedStatementDiscardThreshold|Integer|10|Controla cómo muchas acciones de descarte de instrucción preparada pendientes (sp_unprepare) pueden ser pendientes por conexión antes de ejecuta una llamada a limpiar los controladores pendientes en el servidor. Si el valor es < = 1 anular-preparar las acciones se ejecutará inmediatamente a cerrar la instrucción preparada. Si se establece en > 1 estas llamadas se puede procesar por lotes juntos para evitar una sobrecarga de la llamada a sp_unprepare con demasiada frecuencia.|
|enablePrepareOnFirstPreparedStatementCall|boolean<br /><br /> ["true" &#124;" False"]|false|Si esta configuración está establecida en false la primera ejecución de una instrucción preparada llamaremos sp_executesql y no prepara una instrucción, una vez que la segunda ejecución sucede lo llamaremos sp_prepexec y un identificador de instrucción preparada realmente de la instalación.|
|statementPoolingCacheSize|Integer|10|Puede configurables instrucciones puede almacenar para su reutilización en grupo| 
  
> [!NOTE]  
>  El [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] toma los valores predeterminados para las propiedades de conexión salvo para ANSI_DEFAULTS e IMPLICIT_TRANSACTIONS servidor. El [!INCLUDE[jdbcNoVersion](../../includes/jdbcnoversion_md.md)] establece automáticamente ANSI_DEFAULTS en ON e IMPLICIT_TRANSACTIONS en OFF.  

> [!Note]
> Importante: Si la autenticación se establece en ActiveDirectoryPassword, la biblioteca siguiente debe incluirse en la ruta de clase: [azure biblioteca de Active Directory para java](https://github.com/AzureAD/azure-activedirectory-library-for-java). Se puede encontrar en [Maven repositorio](https://mvnrepository.com/artifact/com.microsoft.azure/adal4j). La manera más sencilla de descargar la biblioteca y sus dependencias está usando a Maven: 
 >1. En primer lugar, instale a Maven en el sistema 
 >2. Vaya a la [página de GitHub](https://github.com/Microsoft/mssql-jdbc) del controlador
 >3. Descargue el archivo pom.xml
 >4. Ejecute el siguiente comando de Maven para descargar la biblioteca y sus dependencias: dependencia: la copia de mvn-dependencias
  
## <a name="see-also"></a>Vea también  
 [Conectarse a SQL Server con el controlador JDBC](../../connect/jdbc/connecting-to-sql-server-with-the-jdbc-driver.md)  
 [Modo FIPS](../../connect/jdbc/fips-mode.md)
  
  

